/** @jsxImportSource @emotion/react */
import { css } from '@emotion/react';

const DMPage = () => {
  return (
    <div
      css={css`
        padding: 2rem;
        font-size: 1.2rem;
      `}
    >
      ✉️ 여기는 DM(메시지 전송) 페이지입니다.
    </div>
  );
};

export default DMPage;
